<%
  # Set the title with a yield because it's dynamic.
  content_for(:title, "#{destination.title} from #{departure.name} - Upon Arrival")
  content_for(:body_class, "locations locations-#{destination.slug}")
%>

<%= partial 'shared/minibar' %>

<%= partial 'shared/tabs' %>

<%= partial 'shared/logo' %>