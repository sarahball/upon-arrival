<%
  # The <title> value
  content_for(:title, "#{departure.title} ✈ #{destination.title} - Upon Arrival")

  # The classes on the `<body>` element
  content_for(:body_class, "result departure-#{departure.slug} destination-#{destination.slug}")
%>

<%= partial 'shared/minibar' %>

<%= partial 'shared/content' %>

<%= partial 'shared/logo' %>
