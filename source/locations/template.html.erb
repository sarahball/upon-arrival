<%
  # The <title> value
  content_for(:title, "#{departure.title} ✈ #{destination.title} - Upon Arrival")

  # The classes on the `<body>` element
  content_for(:body_class, "locations departure-#{departure.slug} destination-#{destination.slug}")
%>

<%= partial 'shared/minibar' %>

<%= partial 'shared/tabs' %>

<%= partial 'shared/logo' %>
